server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # allow API calls to Langfuse (optional)
    location /api/ {
        proxy_pass http://host.docker.internal:3000;
    }
}
